dependencies:
    pre:
        - echo y | android update sdk --no-ui --all --filter "tools"
        - echo y | android update sdk --no-ui --all --filter "android-26"
        - echo y | android update sdk --no-ui --all --filter "build-tools-26.0.2"
        - echo y | android update sdk --no-ui --all --filter "platform-tools"
        - echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"
        - echo y | android update sdk --no-ui --all --filter "extra-android-support"
        - echo y | android update sdk --no-ui --all --filter "extra-google-m2repository"
        - echo y | android update sdk --no-ui --all --filter "extra-google-google_play_services"
        - sudo echo y | android update sdk --no-ui --all
        - sudo chmod +x gradlew
        - ./gradlew androiddependencies


test:
  override:
    # start the emulator
    - emulator -avd circleci-android24 -no-window:
        background: true
        parallel: true
    # wait for it to have booted
    - circle-android wait-for-boot
    - sleep 30
    - adb shell input keyevent 82
    # run tests  against the emulator.
    - ./gradlew lint connectedAndroidTest
     #copy the build outputs to artifacts
    - cp -r /home/ubuntu/AndroidDemo2/app/build/reports/* $CIRCLE_ARTIFACTS
     #copy the test results to the test results directory.
    - cp -r /home/ubuntu/AndroidDemo2/app/build/reports/androidTests/* $CIRCLE_TEST_REPORTS
